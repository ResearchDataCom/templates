"""Top-level tests.

This helps keep selected integration tests outside the source tree.
Container images can then perform testing at build time without having
to mark (and exclude) integration tests that use said container
images.

"""
